import React from 'react';
import { Typography } from '@mui/material';

const Welcome = () => {
    return (
        <div>
            <Typography variant="h1">Welcome to Booker!</Typography>
        </div>
    );
};

export default Welcome;
